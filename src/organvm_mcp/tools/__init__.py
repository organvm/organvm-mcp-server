"""MCP tool implementations — one module per tool group.

Each module exposes functions that the server registers as MCP tools.
Tool functions accept simple JSON-serializable arguments and return
JSON-serializable dicts.
"""
